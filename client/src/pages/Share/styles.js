const styles = ({ theme }) => {
    return {
        shareformcontainer: {
            display: "flex",
            marginTop: 100,
        },
        preview: {
            width: 400,
        },
    }
}

export default styles;
